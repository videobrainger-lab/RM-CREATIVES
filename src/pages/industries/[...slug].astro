---
import SeoIndustryPage from '../../layouts/SeoIndustryPage.astro';
import { enIndustryPages } from '../../data/industryPages';
export function getStaticPaths(){return enIndustryPages.map(page=>({params:{slug:page.slug},props:{page}}));}
const {page}=Astro.props;
---
<SeoIndustryPage {page}/>
